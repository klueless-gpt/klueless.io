---
// Access props passed to the component
const { href } = Astro.props;
// This took two atempts, I had to train it on slots
// and then chastise it the second time.
---

<a href={href} class="inline-block rounded-lg px-2 py-1 text-sm text-slate-700 hover:bg-slate-100 hover:text-slate-900">
  <slot />
</a>

